apiVersion: v1
kind: Service
metadata:
  name: {{ include "magellan.fullname" . }}
  labels:
    {{- include "magellan.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "magellan.selectorLabels" . | nindent 4 }}
