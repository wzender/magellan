{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "magellan.serviceAccountName" . }}
  labels:
    {{- include "magellan.labels" . | nindent 4 }}
{{- end }}
